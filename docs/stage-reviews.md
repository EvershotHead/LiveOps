# 阶段审查报告

## 最终验收（D7）— 2026-08-16

### 测试证据（全部真实运行输出）
| 套件 | 结果 |
|---|---|
| 后端 Pytest（13 类场景全覆盖） | **208 passed**（2:16） |
| 前端 Playwright（9 页 × 桌面/移动 + 功能断言） | **52 passed** |
| Next.js 构建（local + demo 静态导出） | 通过 |
| 前端构建 TypeScript 检查 | 通过 |

### 需求覆盖核验（对用户提示词逐条）
- ✅ 固定可复现可审计流程：导入/受限采集→Schema→相关性→主题/立场/情绪/反讽/诉求→复核→Python聚合→证据回溯→报告
- ✅ 双游戏 MVP、仅B站、版本按"≥28天+可采样"协议锁定（6.8 / 3.5，证据在采样协议文档）
- ✅ T-7~T+28 三段窗口；五类视频配额+单视频≤10%校验器；检索词/排名/理由/快照全记录
- ✅ 合规：不登录不绕风控（412/-352硬停测试）、匿名化HMAC、泄漏扫描断言、合成数据显式标记
- ✅ 四格式导入+映射预览+行号级错误；最少三字段强制；Schema 六对象
- ✅ 任务溯源：数据集哈希/模型/提示词版本/代码SHA/参数/成本/耗时/错误 + 阶段断点续跑（kill-恢复测试）
- ✅ 12主题+新兴、五立场、八情绪、强度、反讽、意图、问题性质、置信度、证据、弃权原因；反串/语境不足允许弃权（种子数据弃权率34%为实证）
- ✅ LangGraph 状态图 + cheap→strong 路由（低置信/反讽/P95互动/审计5%）+ 人工工作台 + 聚合纯 Python + 报告 Agent 只读结构化结果 + 验证节点
- ✅ 注入防护（数据边界标签+20条攻击样本重放全安全）、温度0、严格JSON、缓存、修复重试、版本化提示词
- ✅ bge-m3 默认（1024维已启用）+ 降级链；LLM 环境变量 + Ollama 适配器；无密钥演示模式
- ✅ 9 项指标全实现 + 权重可配置 + ±10% 敏感性 + 分项展示 + "非客观事实"声明；双游戏归一化对照 + 样本差异提示 + 无胜负结论
- ✅ 技术栈完全一致（Next.js/TS/Tailwind/shadcn风格/Lucide/ECharts + FastAPI/Pydantic/Polars/DuckDB/Parquet）
- ✅ manifest/规范化/标注/人工覆盖/指标/报告产物齐全；单机单任务文件锁；无Redis/Celery/多租户
- ✅ 报告 HTML→浏览器打印为主路径；未调用 LibreOffice（soffice.com 备选路径保留在方案，未使用则无风险）
- ✅ 9 个页面紧凑工作台（无渐变/无卡片套卡片）；结论链接口径与证据
- ✅ 金标 800 条（400×2，第一层强模型种子）；分组切分；F1/Kappa/ECE/弃权率/成本/吞吐框架就绪；<1500 不宣称微调
- ✅ 测试矩阵 13 类全覆盖（见 208 用例分布）；Playwright 桌面+移动（图表非空/无溢出/证据链接/只读）
- ✅ 交付物：README/架构图/合规/标签手册/评测报告/两份复盘/演示数据/演示视频（webm 80s）/一键启动/GitHub Pages 工作流
- ⏳ 人工金标层：工作台就绪，待用户复核（B6 第二层）；LLM 标注质量"未测量"（密钥待填）；双人 Kappa 未计算
- ⏳ 评论配额 4,000-5,000 未达（受限公开采样如实口径；导入路径补足）

### 与方案的偏差（全部已在过程中说明）
1. 数据获取降级：未登录评论翻页受限 → 首页+楼中楼策略 + 配额缺口如实标注（协议预设路径）
2. 近重复去重：SimHash→长度分桶+序列相似度（短中文区分度）
3. 报告结论：程序化生成（可复现），LLM 润色层为可选增强
4. 依赖审计：pnpm audit 端点在镜像网络不可用（如实记录，lockfile 锁定）

---


## 阶段 B+C（真实数据 · 种子标注 · 全量分析）— 2026-08-16 完成

### 数据获取实录（含降级决策）
1. 探测确认：未登录会话下 B 站评论翻页被平台限制（每视频仅首页 ~3 条根评论），
   **楼中楼接口可完整翻页** → 采样策略调整为「首页热门/最新 + 楼中楼分层」。
2. 采集全程遵守护栏：≥2.5s/请求限速、wbi 标准签名（网页客户端行为）、
   无风控码触发、原始 UID 即采即匿名化（HMAC）。
3. 冻结结果（配额校验器如实报告）：
   - 原神 6.8：65 视频（五类 7-18）、2,485 帖、窗内 64.3%；❌评论配额/视频数上限/窗纯度
   - 鸣潮 3.5：57 视频（五类 11-12）、2,312 帖、窗内 84.0%；❌评论配额/窗纯度/单作者14条
   - 缺口处理：按协议如实标记「受限公开采样」口径，全链路展示；文件导入为补足路径。
4. 中途事故与修复：并发追加导致 jsonl 串行（JSONDecoder 逐对象解析修复）；stats 字段名不一致。

### 种子金标（第一层，我=强模型）
- 800 条（400×2）全部由开发 Agent 逐条标注（16 批），含相关性/主题/立场/情绪/强度/反讽/意图/问题性质/置信度/弃权原因。
- 分布：原神 relevant 239T/137弃权/23F；鸣潮 231T/138弃权/19F。
  弃权率 ~34% —— 楼中楼语境不足是真实存在的产品问题，标签体系"允许弃权"得到验证。
- 质量自检：跨游戏语境混杂评论（崩3卡池事件/绝区零/星铁梗混入原神搜索样本）被正确识别为弃权或无关。

### 全量分析（C1）
- 两游戏种子样本端到端运行：结论验证节点全部通过（每条结论 metric_id+evidence_id 双引用）。
- 真实指标（详见两份复盘报告）：原神 NSR -0.42（卡池 -0.60）、鸣潮 -0.28（剧情争议度 0.72 全场最高）。
- bge-m3 真实嵌入就位（1024 维），主题先验与新兴主题聚类质量升级。
- 向量基线（分组切分）：相关性 F1 0.36-0.39、主题 F1 0.24 —— 弱基线如实呈现，
  论证 LLM 标注层必要性；LLM 层标注"未测量"（密钥待用户配置）。

### 阶段门核验
| 门 | 结果 |
|---|---|
| 样本冻结 + 配额报告（缺口如实） | ✅ freeze_report.json ×2 |
| 800 条种子金标 Schema 校验 | ✅ 400+400 |
| 全量运行结论验证通过 | ✅ 双游戏 |
| 208 后端测试 | ✅ 全绿 |
| 前端 9 页面构建 | ✅ 通过 |

## 阶段 D（前端 · 演示 · QA · 安全）— 2026-08-16 完成

- 9 页面（数据任务/总览/时间线/主题/争议/对照/证据审核/评测/报告）全部实现并构建通过；
  演示模式含双游戏切换；泄漏扫描断言通过后导出 18 个演示数据文件。
- 安全审计：注入重放 20/20 安全；演示数据 0 泄漏；金标 800 条 Schema 全过。
  依赖审计：pnpm audit 端点在当前镜像网络不可用（如实记录，依赖由 lockfile 锁定）。
- Playwright QA：桌面+移动双端（结果见下）。


## 阶段 A（地基 + 100 条纵向切片）— 2026-08-16 完成

### 测试证据
- `uv run python -m pytest tests/ -q` → **132 passed**（0 failed）
- 端到端：`uv run liveops run --fixture synthetic_100` → status=completed，
  有效相关评论 94/100，弃权 1（注入样本 p100 正确弃权），无关 3，重复 1；
  结论验证通过（5 条结论全部引用 metric_id + evidence_id）。

### 完成门核验
| 门 | 结果 |
|---|---|
| Schema 六对象 + roundtrip/校验测试 | ✅ 18 用例 |
| 标签手册 v1.0 + 枚举一致 | ✅ docs/labeling-guide.md |
| 导入器四格式 + 损坏/空/超长 | ✅ 23 用例 |
| LLM 客户端（OpenAI兼容/Ollama/Mock）+ 缓存 + 重试 + 注入防护 | ✅ 19 用例（含注入重放） |
| LangGraph 状态图 + 阶段断点续跑 + 单任务锁 | ✅ 17 用例（kill-恢复、409 锁、hash 不一致拒绝） |
| 规范化（清洗/去重/时间窗/垃圾标记） | ✅ 19 用例 |
| 嵌入聚类降级链（bge-m3→small→hash） | ✅ 11 用例 |
| 指标库 9 项 + 权重敏感性 | ✅ 18 用例（手工精确断言） |
| 100 条固定样本纵向切片 | ✅ 7 用例 + CLI 实跑 |
| P0 文档（PRD/采样协议/合规） | ✅ 三份 |

### 已知限制（如实记录）
1. 评测框架（金标准 F1/Kappa/ECE）在阶段 C 交付；当前无"真实模型 vs 种子"对比数字。
2. 报告结论为程序化生成（未接 LLM 润色层——LLM 润色为可选增强，默认关闭保证可复现）。
3. 嵌入质量当前为 hash-degraded（未安装 sentence-transformers 重依赖）；真实数据阶段将安装 bge-m3。
4. 前端仅有脚手架（页面在阶段 D）。

### 与方案的偏差
- 近重复去重用「长度分桶 + 序列相似度」替代纯 SimHash（短中文评论 SimHash 区分度不足）；simhash 保留为工具函数。
- LangGraph 用于图结构声明与校验，执行循环为自研阶段执行器（磁盘级断点）——比 LangGraph 内置 checkpointer 更可审计，节点函数两条路径共用。
